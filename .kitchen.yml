---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  # - name: centos-6.4

suites:
  - name: default
    run_list:
      - recipe[web::default]
      - recipe[nodejs::nodejs]
      - recipe[web::npm_packages]
      # - recipe[web::clone_repo]
    attributes: